import { KeysFirebasePipe } from './keys-firebase.pipe';

describe('KeysFirebasePipe', () => {
  it('create an instance', () => {
    const pipe = new KeysFirebasePipe();
    expect(pipe).toBeTruthy();
  });
});
